{{- if ne .GetImageRepository "" }}
image:
  repository: {{ .GetImageRepository }}/workflow_service
{{- end }}

{{- if .IsThinMode }}
resources:
  limits:
    # cpu: 100m
    memory: 2Gi
  requests:
    # cpu: 100m
    memory: 2Gi
{{- end }}